name: app_gastos_tp3_grupo8
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1 # o la versión más reciente
  fl_chart: ^0.64.0
  http: ^1.4.0
  shared_preferences: ^2.0.15
  intl: ^0.17.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  go_router: ^15.1.1

flutter:
  uses-material-design: true
